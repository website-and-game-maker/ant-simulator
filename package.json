{
  "name": "ant-simulator",
  "private": true,
  "version": "0.1.0",
  "description": "A realistic, high-performance ant colony simulator with pheromone trails, genetics, multi-colony warfare, and adjustable graphics/processing power.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc -b --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.5"
  }
}
